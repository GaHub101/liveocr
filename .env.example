# Apps Script Webhook URL
# 1. Google Sheet öffnen → Extensions → Apps Script
# 2. Code aus apps-script/Code.gs einfügen
# 3. Deploy → New deployment → Web app → Execute as: Me, Access: Anyone
# 4. Deployment URL hier eintragen:
VITE_APPS_SCRIPT_URL=https://script.google.com/macros/s/DEINE_DEPLOYMENT_ID/exec

# Shared Secret – schützt den Webhook vor unbefugtem Zugriff
# Beliebiger langer zufälliger String (z.B. aus: openssl rand -hex 32)
# Muss identisch in Apps Script Script Properties als WEBHOOK_SECRET gesetzt werden
VITE_WEBHOOK_SECRET=dein-geheimes-token

# Für lokale Entwicklung: Datei als .env.local speichern (nicht committen!)
# Für GitHub Pages: beide Werte als Secrets APPS_SCRIPT_URL und WEBHOOK_SECRET hinterlegen
